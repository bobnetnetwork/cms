export enum DashboardRoutesEnum {
    ARTICLES = "/articles",
    PAGES = "/pages",
    CATEGORIES = "/categories",
    TAGS = "/tags",
    MEDIA = "/file",
    USERS = "/users",
    ROLES = "/roles",
    SETTINGS = "/settings"
}